# This file will try to hold and save values 
# in the CSM tab of the GUI, it will then 
# transport the values to file to be 
# read by the lpgbt code 

# class CSMreg(object): 

# 	# def __init__(self,ser):
#  #        self.ser = ser

# 	def ePortTX(self): 
# 		#data rate 
# 		self.g3_datarate = ['0xc0'] #group 3 datarate mask 
# 		self.g2_datarate = ['0x3'] #group 2 datarate mask 
# 		self.g1_datarate = ['0x0c'] #group 1 datarate mask 
# 		self.g0_datarate = ['0x0c'] #group 0 datarate mask

# from TDC_config_low_level_function import *
# from ASDreg import *

# class CSMreg(object):

# 	def__init__(self):

# 	def ePortTX(self):
# 		self.g3_datarate = ['0xc0'] #group 3 datarate mask 
# 		g2_datarate = ['0x3'] #group 2 datarate mask 
# 		g1_datarate = ['0x0c'] #group 1 datarate mask 
# 		g0_datarate = ['0x0c'] #group 0 datarate mask 
g3_datarate = ['0xc0'] #group 3 datarate mask 
g2_datarate = ['0x3'] #group 2 datarate mask 
g1_datarate = ['0x0c'] #group 1 datarate mask 
g0_datarate = ['0x0c'] #group 0 datarate mask 